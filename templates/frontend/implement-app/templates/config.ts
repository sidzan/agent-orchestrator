/**
 * Template: Column config for DatagridConfigurable
 * Copy to: apps/<APP>/src/pages/<featureName>/config.ts
 * Replace: field names
 */

export const FIXED_COLUMNS = ['name']; // columns always visible

export const OPTIONAL_COLUMNS = [
  // 'createdDate', 'status', etc. — columns hidden by default
];
