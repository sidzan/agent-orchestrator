-- EDIT-ME
-- Starter template for a SQL migration. Replace with your project's conventions
-- (naming, schema, audit columns, multi-tenant scope, etc.). The orchestrator
-- references this asset in the migration step — keep the file path stable,
-- change the contents.

-- TODO: replace with your create-update-trigger template
